import type { Config } from "tailwindcss";

const config: Config = {
  content: [
    "./client/**/*.{js,ts,jsx,tsx,html}"
  ],
  theme: {
    extend: {},
  },
  plugins: [],
};
export default config;
